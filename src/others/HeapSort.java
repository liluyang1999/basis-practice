package others;

/** Historical entry point; the maintained heap-sort implementation is in sort.HeapSort. */
public class HeapSort {
    public static void sort(int[] arr) { sort.HeapSort.sort(arr); }
    public static void main(String[] args) { sort.HeapSort.main(args); }
}
